# Find Solutions plugin

This dual-host plugin contains one explicit skill: `find-solutions`.

- Codex metadata: `.codex-plugin/plugin.json`
- Claude Code metadata: `.claude-plugin/plugin.json`
- Shared Agent Skills implementation: `skills/find-solutions/`

The skill is research-only. It never posts, buys, books, contacts, deploys, or changes external state.
